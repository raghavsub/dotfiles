# /usr/bin/env bash
#
# Install dotfiles

main()
{
    cd dotfiles
    find -type f | sed "s/^\.\///" | while read line; do
        echo "Copying "$line"..."
        cp "$PWD"/"$line" "$HOME"/."$line"
    done
}

main
